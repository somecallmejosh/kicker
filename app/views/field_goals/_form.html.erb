<%= form_for(@field_goal) do |f| %>
  <% if @field_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@field_goal.errors.count, "error") %> prohibited this field_goal from being saved:</h2>

      <ul>
      <% @field_goal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>
  <div>
    <%= f.label :distance, "Disance (yards)" %>
    <%= f.number_field :distance %>
  </div>
  <div>
    <%= f.check_box :is_good  %>
    <%= f.label :is_good,'Field Goal Good?' %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit', :class => "button expand" %>
  </div>
<% end %>
