<% provide(:title, kicker_name + " | PAT Stats") %>
<div class="row">
  <div class="large-10 medium-10 columns">
    <h1>PAT <small>Stats</small></h1>
  </div>
  <div class="large-2 medium-2 columns">
    <% if user_signed_in? %>
      <%= link_to 'New PAT', new_point_after_attempt_path, :class => "button expand" %>
    <% end %>
  </div>
</div>
<div class="row">
  <div class="large-6 columns">
    <div class="panel">
      <strong>PAT Points:</strong> <%= @totalpoints %>
    </div>
  </div> 
  <div class="large-6 columns">
     <div class="panel">
       <strong>PAT Success:</strong> <%= @pat_percent %>%
     </div>
   </div> 
</div>
<div class="row">
  <div class="large-12 columns">
    <table>
      <thead>
        <tr>
        <th>Date</th>
        <th>PAT Good?</th>
        <% if user_signed_in? %>
          <th></th>
          <th></th>
        <% end %>
      </tr>
      </thead>

      <tbody>
        <% @point_after_attempts.each do |point_after_attempt| %>
      <tr>
        <td><%= l(point_after_attempt.date) %></td>
        <td><%= point_after_attempt.is_good ? 'Yes' : 'No'  %></td>
        <% if user_signed_in? %>
          <td><%= link_to raw('<i class="fa fa-pencil"></i>'), edit_point_after_attempt_path(point_after_attempt), :class => "button tiny expand"  %></td>
          <td><%= link_to raw('<i class="fa fa-times-circle"></i>'), point_after_attempt, method: :delete, data: { confirm: 'Are you sure?' }, :class => "button tiny expand"  %></td>
        <% end %>
      </tr>
    <% end %>
      </tbody>
    </table>
    <% if user_signed_in? %>
      <%= link_to 'New PAT', new_point_after_attempt_path, :class => "button secondary" %>
    <% end %>
  </div>
</div>
